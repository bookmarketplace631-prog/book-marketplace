# PostgreSQL Database Configuration
DB_USER=postgres
DB_PASSWORD=your_database_password
DB_HOST=your_railway_postgres_host
DB_PORT=5432
DB_NAME=bookmarketplace

# Node Environment
NODE_ENV=production
PORT=3000

# Optional: For local development, use localhost
# DB_HOST=localhost
